diff --git a/node_modules/@cornerstonejs/core/dist/esm/RenderingEngine/StackViewport.js b/node_modules/@cornerstonejs/core/dist/esm/RenderingEngine/StackViewport.js
index 1d586a7e1b..f6f0a42f8c 100644
--- a/node_modules/@cornerstonejs/core/dist/esm/RenderingEngine/StackViewport.js
+++ b/node_modules/@cornerstonejs/core/dist/esm/RenderingEngine/StackViewport.js
@@ -1841,8 +1841,13 @@ class StackViewport extends Viewport {
     }
     _getImagePlaneModule(imageId) {
         const imagePlaneModule = getImagePlaneModule(imageId);
+        const hasExplicitPixelSpacing = Array.isArray(imagePlaneModule.pixelSpacing) &&
+            imagePlaneModule.pixelSpacing.length >= 2 &&
+            imagePlaneModule.pixelSpacing[0] > 0 &&
+            imagePlaneModule.pixelSpacing[1] > 0;
         this.hasPixelSpacing =
-            !imagePlaneModule.usingDefaultValues ||
+            hasExplicitPixelSpacing ||
+                !imagePlaneModule.usingDefaultValues ||
                 this.calibration?.scale > 0 ||
                 this.calibration?.rowPixelSpacing > 0;
         this.calibration ||= imagePlaneModule.calibration;
diff --git a/node_modules/@cornerstonejs/core/dist/esm/RenderingEngine/BaseVolumeViewport.js b/node_modules/@cornerstonejs/core/dist/esm/RenderingEngine/BaseVolumeViewport.js
index aaaaaaaaaa..bbbbbbbbbb 100644
--- a/node_modules/@cornerstonejs/core/dist/esm/RenderingEngine/BaseVolumeViewport.js
+++ b/node_modules/@cornerstonejs/core/dist/esm/RenderingEngine/BaseVolumeViewport.js
@@ -522,10 +522,19 @@ class BaseVolumeViewport extends Viewport {
         });
         this.render();
     }
-    setViewReference(viewRef) {
+    setViewReference(viewRef, _nolexDepth = 0) {
         if (!viewRef) {
             return;
         }
+        // [nolex patch] Recursion guard: when the dataset has an oblique
+        // acquisition the view-plane normal cannot be aligned exactly to the
+        // viewport normal (float precision), so isSameNormal stays false on
+        // the recursive call below and the function explodes with
+        // "Maximum call stack size exceeded". Bail out gracefully after one
+        // re-orientation attempt.
+        if (_nolexDepth > 1) {
+            return;
+        }
         const volumeId = this.getVolumeId();
         const { viewPlaneNormal: refViewPlaneNormal, FrameOfReferenceUID: refFrameOfReference, cameraFocalPoint, referencedImageId, viewUp, } = viewRef;
         let { sliceIndex } = viewRef;
@@ -547,7 +556,7 @@ class BaseVolumeViewport extends Viewport {
         else if (refFrameOfReference === this.getFrameOfReferenceUID()) {
             if (refViewPlaneNormal && !isNegativeNormal && !isSameNormal) {
                 this.setOrientation({ viewPlaneNormal: refViewPlaneNormal, viewUp });
-                this.setViewReference(viewRef);
+                this.setViewReference(viewRef, _nolexDepth + 1);
                 return;
             }
             if (cameraFocalPoint) {
